# Release folder

Built artefacts for AgentSpace `v0.1.0`, ready to upload by hand.

**Start with [PUBLISH.md](PUBLISH.md)** — it says what each file is and what
to click.

Nothing here is tracked by git; `.gitignore` excludes `release/`. Rebuild it
any time with the commands at the bottom of PUBLISH.md.
